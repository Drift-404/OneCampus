# Campus Connect Lite - Frontend (Vite + React + Tailwind)
## Quick start
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Open http://localhost:5173
## Notes
- The frontend expects the backend API at http://localhost:5000
